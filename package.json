{
	"name": "win",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"start:api": "bun run --filter api start",
		"start:web": "bun run --filter web dev",
		"start:mobile": "bun run --filter mobile start",
		"start:hero": "bun run --filter hero dev",
		"start:desktop": "bun run --filter web tauri:dev",
		"db:generate": "bun run --filter @wingmnn/db db:generate",
		"db:migrate": "bun run --filter @wingmnn/db db:migrate",
		"db:push": "bun run --filter @wingmnn/db db:push",
		"db:studio": "bun run --filter @wingmnn/db db:studio",
		"start:redis": "redis-server"
	},
	"devDependencies": {
		"@types/bun": "^1.3.9"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"better-auth": "^1.4.18",
		"zod": "^4.3.6"
	}
}
